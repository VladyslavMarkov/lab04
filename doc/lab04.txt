Лабораторна робота №4. Розробка програм, що розгалужуються

Марков Владислав Андрійович, гр. КН-922Б 

Завдання: Визначити, чи існує ромб з заданими сторонами a, b, c, d.

Основна частина:

- опис роботи основної функції: У ромба всі сторони рівні a = b = c = d.

#include <stdio.h>

int main()
{   
    //Сторони ромба
    int a,b,c,d,res;
    
    a = 10;
    b = 10;
    c = 10;
    d = 10;
    res = 0;

    //Перевіряємо рівні сторони ромба чи ні
    if(a == b && a == c && a == d && res == 0)
    {
     //Якщо сторони рівні, то привласнюємо значення 1 змінній res
      res = 1;
    }
               
    return 0;
}

- дослідження результатів роботи програми:

-> 21       return 0;
   22   }
(lldb) p res
(int) $0 = 1

-----------------------------------------------------------------------------

- для підтвердження коректності роботи програми, зупинено відлагодник на строці зі змінною "b = 10;" та введемо команду "p a = 1". Після вводимо команду "c" і отримуємо наступне:

-> 21       return 0;
   22   }
(lldb) p res
(int) $2 = 0

--------------------------------------------------------------------

Структура проекту лабораторної роботи:
   
├── lab04
│   ├── doc
│   │   └── lab04.txt
│   ├── Makefile
│   ├── README.md
│   └── src
│       └── main.c

Висновки: при виконанні лабораторної роботи були набуті практичні навички створення програм, що розгалужуються на мові С, зокрема: завдання умов, написання дій, якщо умова виконується і навпакию множення, та зміна результатів роботи програми на льоту.

